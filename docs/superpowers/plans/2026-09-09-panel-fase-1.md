# Panel del cliente · Fase 1, Parte A — La maquinaria de contenido

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir la maquinaria isomorfa que valida y describe el contenido —constructores, cargador, serializador, derivados y validación— con sus tests, lista para que la Parte B declare los esquemas y migre el contenido real.

**Por qué en dos partes.** La Parte B son ~900 líneas de esquema escritas a mano contra esta API. En la Fase 0, seis hallazgos Importantes fueron tests que el plan traía escritos contra una API que yo solo había imaginado. Escribir la Parte B **después** de que esta maquinaria exista, esté revisada y tenga su forma final es estrictamente más exacto que escribirla ahora. La Parte B se planifica cuando la A cierre.

**Architecture:** Cuatro JSON en `src/contenido/datos/` describen el contenido. Su forma vive en un esquema Zod cuyos constructores **exigen etiqueta y ayuda en español**: un campo sin etiqueta no compila. Ese registro es el único catálogo de campos del sistema — lo va a leer el panel para pintarse, la función para rechazar, vitest para verificar y `astro build` para no publicar basura. Las tres fachadas (`src/copy/sitio-marca.ts`, `src/copy/sabores.ts`, `src/fichas/base.ts`) pasan de 946 líneas a ~36 y re-exportan exactamente el mismo objeto que hoy.

**Tech Stack:** Zod 4.4.3 (fijada por override), Astro 7 estático, Vitest 4, TypeScript 6, pnpm 11.2.2.

**Spec:** `docs/superpowers/specs/2026-09-08-panel-cliente-design.md` — secciones `1`, `2` y `10` de la arquitectura.

**Entrega de la Parte A:** `src/contenido/` completo y probado, sin ningún consumidor todavía. El sitio no cambia: `pnpm build` sigue verde y `index.astro` sigue leyendo los módulos `as const` de hoy.

**Base:** rama `panel-fase-1`, creada desde `origin/main` en `6c9113c` (Fase 0 mergeada y verificada en producción).

## Global Constraints

Valen para toda tarea de este plan.

- **REGLA DURA DE LA CARPETA:** `src/contenido/**` no importa `node:*`, no importa Astro, y usa **solo rutas relativas sin extensión** (nada de `@/`). Es la condición para que el mismo código corra en el navegador de la clienta, en la función serverless y en vitest. Dependencias externas permitidas: `zod`, `../tokens/color` y `../tokens/contrast` (los dos son puros, verificado). La Tarea 1 instala el guard que lo vigila.
- **Registro es-MX.** Vocabulario prohibido de MARCA en todo copy: «mono», «chango», «changuito», «chispa(s)», «carrito», «pistachos», «cacahuete», «maní», «packaging», «snack», «smoothie». La lista de MAQUETA («Borrador», «PENDIENTE», «te avisamos», «Lorem») aplica **solo al sitio publicado**, nunca a los mensajes del panel — que necesita la palabra «Borrador» para su concepto central.
- **Precios como número entero**, 1–99.999. Ningún `$` seguido de dígito dentro de un string de copy.
- **Colores solo desde tokens** (`src/tokens/color.ts`). Ningún hex a mano.
- **El sitio queda 100% estático.** Ningún adapter, ningún `output: 'server'`.
- **Cero `.astro` tocados en toda la fase.** Por higiene de señal: un commit, una causa. (La justificación vieja —que los `data-astro-cid-*` dependen del contenido— es falsa y está verificada como tal en el spec §10.)
- **Comentarios en español rioplatense y explican POR QUÉ**, no qué. Las etiquetas y ayudas que ve la clienta van en **español mexicano**, sin jerga.
- **Línea de base:** hoy `pnpm test` da **683 tests verdes** (29 archivos) y `pnpm typecheck` **0 errores, 0 warnings, 3 hints**. Ninguna tarea puede bajar el verde. El número de tests sube; lo que no cambia es que estén todos en verde.
- **`pnpm build` es la compuerta** (Fase 0): construye, corre la suite y `astro check`. Corrélo antes de cada commit que toque el camino del import.
- **Commits en español, imperativo,** terminando con:

  ```
  Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
  ```

### Regla nueva de método, y es la más importante de este plan

En la Fase 0, **seis de los hallazgos Importantes fueron tests que el plan traía escritos y que no probaban nada**: uno no detectaba la regresión que decía cubrir, otro congelaba contenido en la fase que existe para descongelarlo, otro cubría tres de cuatro campos, y hasta un arreglo dictado estaba roto.

Por eso, en este plan:

> **Todo test que se escriba tiene que venir con su prueba de que falla.** No alcanza con verlo pasar. Hay que romper a propósito lo que el test dice cuidar, verlo dar **rojo**, restaurar, y pegar las dos salidas en el reporte. Un test que pasa antes y después de la implementación no prueba nada, y es exactamente el error que esta fase no se puede permitir: acá se construye el esquema del que van a colgar las cinco fases siguientes.

Si un paso de este plan te hace escribir un test y **no** te dice cómo probar que falla, eso es un defecto del plan: paralo y decilo.

---

## File Structure

**Se crean:**

| Archivo | Responsabilidad |
|---|---|
| `src/contenido/vocabulario.ts` | Las dos listas de palabras prohibidas: `MARCA` y `MAQUETA`. |
| `src/contenido/campos.ts` | `MetaCampo`, el registro `panel`, y los ~18 constructores. El corazón del sistema. |
| `src/contenido/carga.ts` | `recorre()` (lo único que toca interna de Zod), `cargar()` y `serializa()`. |
| `src/contenido/conteos.ts` | La regla `cuenta` y la tabla de 1 a 20 en letras. |
| `src/contenido/color-sabor.ts` | `resuelveColor()`, `mejorTinta()` y la regla de contraste ≥4.5. |
| `src/contenido/derivados.ts` | Los precios que se calculan y la clienta no edita. |
| `src/contenido/validacion.ts` | `validar(doc, crudo) → Problema[]` — una verdad, cuatro consumidores. |
| `src/contenido/esquema/sitio.ts` | Los ~300 campos de la home, anotados. |
| `src/contenido/esquema/sabores.ts` | Sabores, gotas, polvo. |
| `src/contenido/esquema/fichas.ts` | Las 4 fichas técnicas. |
| `src/contenido/esquema/index.ts` | `DOCUMENTOS` y `SECCIONES`: de acá se pinta el panel. |
| `src/contenido/datos/*.json` | El contenido. Lo escribe el script de migración, **nadie lo tipea a mano**. |
| `scripts/migra-contenido.ts` | Corre dos veces: una para el fixture, otra para los JSON. |
| `test/fixtures/contenido-2026-09-09.json` | El acta de la migración. Capturado de los módulos viejos. |
| `test/contenido.test.ts` | La regla de la carpeta + el canario de los internals de Zod. |
| `test/contenido-fachada.test.ts` | El certificado (3 deep-equal) + 8 aserciones de forma. |
| `test/contenido-mutaciones.test.ts` | ~25 contenidos malos conocidos que `validar()` tiene que cachar. |

**Se modifican:**

| Archivo | Qué cambia |
|---|---|
| `src/copy/sitio-marca.ts` | 441 líneas → ~16. Pasa a fachada: `marca` y `precioMXN`. |
| `src/copy/sabores.ts` | 101 → ~14. Fachada: `Sabor`, `sabores`, `gotas`, `polvo`, `urlCatalogoBarras`. |
| `src/fichas/base.ts` | 404 → 6. Fachada: `fichasBase`. |
| `test/marca-copy.test.ts` | Lee `envolturas.json` desde su ruta nueva. |

**Se mueve:** `docs/envolturas.json` → `src/contenido/datos/envolturas.json` (el arte de imprenta es dato del sistema, no documentación).

**NO se crean, aunque el spec los liste en la carpeta:** `diff.ts` (`resume()`/`frase()`) es de la bandeja de publicar — Fase 5/6. Meterlo acá sería construir sin consumidor.

---

## Task 1: El vocabulario y la regla de la carpeta

Va primera porque instala la constitución del directorio. Todo lo demás se escribe adentro de esa regla.

**Files:**
- Create: `src/contenido/vocabulario.ts`
- Create: `test/contenido.test.ts`

**Interfaces:**
- Produces: `MARCA`, `MAQUETA`, `palabraProhibida(texto: string): string | null` desde `./vocabulario`. Las consumen `campos.ts` (Tarea 2) y `validacion.ts` (Tarea 14).

- [ ] **Step 1: Escribir el guard de la carpeta, que falla porque la carpeta no existe**

Create `test/contenido.test.ts`:

```ts
/*
 * Guards de la capa de contenido.
 *
 * El primero es la constitución de la carpeta: `src/contenido/**` tiene que
 * poder correr en TRES lugares —el navegador de la clienta, la función
 * serverless y vitest— así que no puede tocar `node:*`, ni Astro, ni el
 * alias `@/` (que solo resuelven el bundler y vitest, no el navegador).
 * Sin este guard, la primera vez que alguien importe `node:fs` para una
 * comodidad, el panel deja de compilar en el navegador y nadie sabe por qué.
 */
import { describe, it, expect } from 'vitest'
import { readFileSync, readdirSync } from 'node:fs'

describe('la capa de contenido', () => {
  it('src/contenido/ no importa node:, ni Astro, ni el alias @/', () => {
    const infractores: string[] = []
    const archivos = readdirSync('src/contenido', { recursive: true, encoding: 'utf8' })

    for (const archivo of archivos) {
      if (!archivo.endsWith('.ts')) continue
      const fuente = readFileSync(`src/contenido/${archivo}`, 'utf8')
      // Los comentarios quedan fuera: este mismo archivo los nombra.
      const codigo = fuente
        .replace(/\/\*[\s\S]*?\*\//g, '')
        .replace(/^\s*\/\/.*$/gm, '')

      const prohibidos = [
        [/from\s+['"]node:/, 'node:'],
        [/from\s+['"]astro/, 'astro'],
        [/from\s+['"]@\//, 'el alias @/'],
      ] as const

      for (const [patron, motivo] of prohibidos) {
        if (patron.test(codigo)) infractores.push(`${archivo} importa ${motivo}`)
      }
    }

    expect(infractores).toEqual([])
  })
})
```

- [ ] **Step 2: Correr el test para verificar que falla**

Run: `npx vitest run test/contenido.test.ts`
Expected: FAIL — `ENOENT: no such file or directory, scandir 'src/contenido'`. La carpeta todavía no existe.

- [ ] **Step 3: Escribir el vocabulario**

Create `src/contenido/vocabulario.ts`:

```ts
/*
 * Las dos listas de palabras prohibidas, separadas a propósito.
 *
 * MARCA salió de la retro del cliente (2026-08-05) y aplica al copy del
 * sitio Y a los mensajes del panel: son palabras que la marca no usa,
 * en ningún lado.
 *
 * MAQUETA es un guard anti-borrador que aplica SOLO al sitio publicado.
 * Si el panel compartiera una lista sola, tendría prohibida la palabra
 * «Borrador» — que es justamente su concepto central.
 */
export const MARCA = [
  'mono',
  'chango',
  'changuito',
  'chispa',
  'carrito',
  'pistachos',
  'cacahuete',
  'maní',
  'packaging',
  'snack',
  'smoothie',
] as const

export const MAQUETA = ['borrador', 'pendiente', 'te avisamos', 'lorem'] as const

// `\b` de JavaScript es ASCII: entre «n» e «í» ve una frontera, así que
// `\bmaní\b` matchearía adentro de cualquier palabra con acento. Se
// declara el alfabeto a mano para que «monocromo» y «carritos del
// súper» no cuenten como la palabra prohibida, pero «monos» y «carrito» sí.
const LETRA = 'a-záéíóúñü'

/** La primera palabra de MARCA que aparece en el texto, o null. */
export function palabraProhibida(texto: string): string | null {
  for (const palabra of MARCA) {
    const re = new RegExp(`(^|[^${LETRA}])${palabra}s?($|[^${LETRA}])`, 'i')
    if (re.test(texto)) return palabra
  }
  return null
}
```

- [ ] **Step 4: Correr el test para verificar que pasa**

Run: `npx vitest run test/contenido.test.ts`
Expected: PASS, 1 test.

- [ ] **Step 5: Probar que el guard de la carpeta detecta de verdad**

Agregá temporalmente a `src/contenido/vocabulario.ts`, como primera línea:

```ts
import { readFileSync } from 'node:fs'
```

Run: `npx vitest run test/contenido.test.ts`
Expected: **FAIL** — `expected [ 'vocabulario.ts importa node:' ] to deeply equal []`.

**Sacá esa línea** y volvé a correr: PASS. Pegá las dos salidas en el reporte.

- [ ] **Step 6: Escribir los tests del vocabulario, con sus casos negativos**

Agregá a `test/contenido.test.ts`, dentro del mismo describe:

```ts
  it('palabraProhibida encuentra la palabra, y no la encuentra donde no está', () => {
    // Las que tiene que cachar, incluidas las formas en plural.
    expect(palabraProhibida('el mono de la envoltura')).toBe('mono')
    expect(palabraProhibida('dos monos')).toBe('mono')
    expect(palabraProhibida('chispas de chocolate')).toBe('chispa')
    expect(palabraProhibida('agregá al carrito')).toBe('carrito')
    expect(palabraProhibida('los carritos')).toBe('carrito')
    expect(palabraProhibida('con pistachos')).toBe('pistachos')
    expect(palabraProhibida('trae maní')).toBe('maní')

    // Las que NO son la palabra prohibida, aunque la contengan. Sin
    // esto, el filtro le prohibiría a la clienta escribir «monocromo».
    expect(palabraProhibida('impresión monocromo')).toBeNull()
    expect(palabraProhibida('un chispazo de sabor')).toBeNull()
    expect(palabraProhibida('la manía de revisar')).toBeNull()
    expect(palabraProhibida('Chocolate con pistaches')).toBeNull()
  })

  it('MAQUETA no contamina al panel: «Borrador» no está en MARCA', () => {
    // El panel necesita esa palabra para su concepto central. Si algún
    // día alguien funde las dos listas, este test lo frena.
    expect(MARCA).not.toContain('borrador')
    expect(MAQUETA).toContain('borrador')
    expect(palabraProhibida('Borrador sin publicar')).toBeNull()
  })
```

Y sumá al import de arriba del archivo:

```ts
import { MARCA, MAQUETA, palabraProhibida } from '../src/contenido/vocabulario'
```

- [ ] **Step 7: Correr y verificar**

Run: `npx vitest run test/contenido.test.ts`
Expected: PASS, 3 tests.

- [ ] **Step 8: Probar que los casos negativos no son decorativos**

Cambiá temporalmente el `LETRA` de `vocabulario.ts` por una frontera ASCII ingenua — reemplazá el cuerpo del `for` por:

```ts
    const re = new RegExp(`\\b${palabra}s?\\b`, 'i')
```

Run: `npx vitest run test/contenido.test.ts`
Expected: **FAIL** — `expected 'maní' to be null` en el caso de «la manía de revisar», porque `\b` corta en la í.

**Restaurá la versión con `LETRA`.** Pegá las dos salidas en el reporte. Esto demuestra que los cuatro casos negativos están probando algo real y no acompañando.

- [ ] **Step 9: Correr la suite completa y commitear**

Run: `pnpm test && pnpm typecheck`
Expected: PASS — 686 tests (683 + 3), typecheck en 0 errores.

```bash
git add src/contenido/vocabulario.ts test/contenido.test.ts
git commit -m "feat: la capa de contenido arranca con su constitución y su vocabulario

src/contenido/** tiene que correr en tres lugares —el navegador de la
clienta, la función serverless y vitest—, así que no puede tocar node:*,
ni Astro, ni el alias @/. El guard lo vigila desde el primer commit, antes
de que haya nada que arreglar.

Las palabras prohibidas van en DOS listas y no en una: MARCA aplica al
copy y a los mensajes del panel; MAQUETA solo al sitio publicado. Con una
sola lista, el panel tendría prohibida la palabra «Borrador», que es su
concepto central.

La frontera de palabra se declara a mano porque el \\b de JavaScript es
ASCII y cortaba adentro de «manía».

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 2: `MetaCampo`, el registro, y los constructores de texto

El corazón del sistema. Lo que hace que **un campo sin etiqueta no compile**.

**Files:**
- Create: `src/contenido/campos.ts`
- Modify: `test/contenido.test.ts`

**Interfaces:**
- Consumes: `palabraProhibida` de `./vocabulario` (Tarea 1).
- Produces: `MetaCampo`, `Seccion`, `ModoFalla`, `panel` (el registro), y los constructores `texto`, `parrafo`, `medida`, `opcion`. Los consumen todos los esquemas (Tareas 9-12) y `validacion.ts` (Tarea 14).

**Tres cosas verificadas contra zod 4.4.3 antes de escribir esta tarea** — no las re-investigues:
1. `z.registry<T>()` existe y `registry.get(esquema)` devuelve el metadato.
2. `.refine()` devuelve un objeto **nuevo**; la base no queda contaminada. Y `_zod.def.type` sigue siendo `'string'` — refinar **no** envuelve el esquema, así que `recorre()` (Tarea 5) no tiene que desenvolver nada.
3. **La trampa:** el registro **sigue la cadena de padres**. Si anotás una base compartida, todo campo derivado de ella hereda esa etiqueta cuando no tiene la propia. Por eso `anota()` recibe el esquema **terminado**, nunca una base, y el Step 6 pone un guard.

- [ ] **Step 1: Escribir el test de que la etiqueta y la ayuda son obligatorias**

Agregá a `test/contenido.test.ts`:

```ts
  it('todo campo anotado tiene etiqueta, ayuda y sección — el panel se pinta de ahí', () => {
    // El panel dibuja cada campo con su etiqueta en español y su oración
    // de «dónde vive». Un campo sin eso es un input mudo: la clienta lo ve
    // y no sabe qué está tocando. Por eso el metadato es obligatorio en el
    // tipo, y este test lo verifica también en runtime — TypeScript no
    // alcanza cuando el esquema se arma con un spread.
    const campo = texto({
      etiqueta: 'Renglón 2 del titular',
      seccion: 'portada',
      ayuda: 'La segunda línea del título grande, arriba de todo.',
      max: 40,
    })
    const meta = panel.get(campo)
    expect(meta?.etiqueta).toBe('Renglón 2 del titular')
    expect(meta?.ayuda).toBeTruthy()
    expect(meta?.seccion).toBe('portada')
    expect(meta?.control).toBe('texto')
  })

  it('el texto rechaza vacío, vocabulario de marca y precios pegados', () => {
    const campo = texto({
      etiqueta: 'Prueba', seccion: 'portada', ayuda: 'x', max: 60,
    })
    expect(campo.safeParse('Chocolate mexicano').success).toBe(true)

    // Vaciar un campo es el gesto más típico de un CMS, y hoy rompe el build.
    expect(campo.safeParse('   ').success).toBe(false)
    // La marca no dice «chispas»: dice gotas.
    expect(campo.safeParse('con chispas de chocolate').success).toBe(false)
    // Los precios los formatea precioMXN; escritos a mano se desincronizan.
    expect(campo.safeParse('desde $108 el paquete').success).toBe(false)
    // Y el techo de cordura corta.
    expect(campo.safeParse('x'.repeat(61)).success).toBe(false)
  })
```

Sumá al import del archivo:

```ts
import { texto, panel } from '../src/contenido/campos'
```

- [ ] **Step 2: Correr para verificar que falla**

Run: `npx vitest run test/contenido.test.ts`
Expected: FAIL — no se resuelve `../src/contenido/campos`.

- [ ] **Step 3: Escribir `campos.ts`**

Create `src/contenido/campos.ts`:

```ts
/*
 * El catálogo de campos del sistema.
 *
 * Cada campo del contenido se declara con uno de estos constructores, y
 * todos EXIGEN etiqueta, sección y ayuda en español. No es burocracia: el
 * panel se pinta leyendo este registro, así que un campo sin etiqueta
 * sería un input mudo. Que el tipo lo exija significa que no se puede
 * agregar un campo al sitio y olvidarse de explicárselo a la clienta.
 *
 * Este archivo corre en tres lugares (navegador, función serverless y
 * vitest), así que no toca `node:*`, ni Astro, ni el alias `@/`.
 */
import { z } from 'zod'
import { palabraProhibida } from './vocabulario'

/** Las secciones tal como las va a ver la clienta en el panel. */
export type Seccion =
  | 'portada'
  | 'productos'
  | 'sabores'
  | 'negocios'
  | 'recetas'
  | 'nosotros'
  | 'catar'
  | 'preguntas'
  | 'contacto'
  | 'pie'
  | 'fichas'
  | 'buscadores'
  | 'accesibilidad'

/**
 * Cómo se rompe el slot si el texto no entra. Lo usa el medidor (fase 4)
 * para decidir qué mirar; 'ninguno' es para los campos que no se ven
 * (alt, aria, el <head>).
 */
export type ModoFalla = 'nowrap' | 'fila' | 'renglones' | 'alto' | 'ninguno'

export interface MetaCampo {
  /** «Renglón 2 del titular». Lo lee la clienta. Obligatorio. */
  etiqueta: string
  seccion: Seccion
  /** La oración de «dónde vive esto en la página». Obligatoria. */
  ayuda: string
  /** Quién lo edita. Default: la clienta. */
  quien?: 'cliente' | 'marcos'
  control?:
    | 'texto' | 'parrafo' | 'precio' | 'medida' | 'renglones'
    | 'lista' | 'foto' | 'regulado' | 'derivado' | 'oculto'
  /**
   * TECHO DE CORDURA, no el límite de diseño. Generoso a propósito: si
   * el texto entra o no lo decide el medidor midiendo la página de
   * verdad (fase 4), porque 31 «W» miden 426 px y 31 «i» miden 142 px.
   * El panel lo dice con esas palabras: «tope de seguridad», no «cabe».
   */
  max?: number
  min?: number
  /** El ':' o el '.' que agrega la plantilla. El panel lo dibuja gris. */
  sufijo?: string
  /** El texto menciona una cantidad que sale de una lista: hay que cruzarla. */
  cuenta?: 'sabores' | 'gotas' | 'polvo' | 'recetas' | 'preguntas'
  /** Rutas hermanas que reciben el mismo valor (el correo vive en cuatro). */
  escribeTambien?: string[]
  /** Cómo nombrar un elemento de lista en el panel («Receta: peras al vino»). */
  nombra?: (v: unknown) => string
  falla?: ModoFalla[]
  /** Vive en un atributo, no en un nodo de texto: 'alt', 'aria-label'… */
  enAtributo?: string
  /** Se renderiza en versales; el medidor tiene que medirlo así. */
  mayusculas?: boolean
}

/**
 * El registro. OJO: `registry.get()` sigue la cadena de padres, así que
 * anotar una base compartida le pone esa etiqueta a todo lo derivado de
 * ella. Por eso `anota()` recibe siempre el esquema TERMINADO.
 */
export const panel = z.registry<MetaCampo>()

const anota = <T extends z.ZodType>(esquema: T, meta: MetaCampo): T => {
  panel.add(esquema, meta)
  return esquema
}

/** Lo que cumple TODO texto visible, sea cual sea su forma. */
const reglasDeTexto = (base: z.ZodString) =>
  base
    .refine((v) => v.trim().length > 0, 'No puede quedar vacío.')
    // OJO: `.refine(pred, (v) => ({message}))` es sintaxis de zod 3 y en la
    // 4.4.3 NO existe: no compila, y en runtime el mensaje queda en «Invalid
    // input» — en inglés y en silencio. Para un mensaje que depende del valor,
    // la forma soportada es `superRefine` + `ctx.addIssue`.
    .superRefine((v, ctx) => {
      const palabra = palabraProhibida(v)
      if (palabra !== null) {
        ctx.addIssue({ code: 'custom', message: `La marca no usa la palabra «${palabra}».` })
      }
    })
    .refine(
      (v) => !/\$\s?\d/.test(v),
      'El precio no se escribe adentro del texto: tiene su propio campo.',
    )

type Base = Omit<MetaCampo, 'control'>

/** Una línea de texto. El caso normal. */
export const texto = (meta: Base & { max: number }) =>
  anota(reglasDeTexto(z.string().trim().max(meta.max)), { control: 'texto', ...meta })

/** Varias oraciones: un párrafo de «Nosotros», la respuesta de una pregunta. */
export const parrafo = (meta: Base & { max: number }) =>
  anota(reglasDeTexto(z.string().trim().max(meta.max)), { control: 'parrafo', ...meta })

/**
 * Cifra + unidad, con espacio duro: «70 g», «250 g».
 *
 * Si la clienta retoca esto en un input común, el espacio duro se vuelve
 * uno normal y en el celular la «g» queda sola en el renglón siguiente.
 * El panel le ofrece el arreglo con un botón; ella nunca se entera de
 * que el espacio duro existe, que es lo correcto.
 */
export const medida = (meta: Base & { max: number }) =>
  anota(
    reglasDeTexto(z.string().trim().max(meta.max)).refine(
      // `\s` matchea TAMBIÉN el espacio duro, así que con él la regla
      // rechazaría el valor bien escrito igual que el mal escrito — y ningún
      // valor de `medida` podría pasar nunca. `[^\S\u00a0]` es «espacio en
      // blanco que NO sea el duro», que es exactamente lo que hay que cazar.
      (v) => !/\d[^\S\u00a0](g|kg|ml|l|°C)\b/.test(v),
      'Entre el número y la unidad va un espacio que no parte el renglón.',
    ),
    { control: 'medida', ...meta },
  )

/** Un valor de una lista cerrada (el `valor` de las opciones del formulario). */
export const opcion = <const V extends readonly [string, ...string[]]>(
  meta: Base & { valores: V },
) => anota(z.enum(meta.valores), { control: 'oculto', quien: 'marcos', ...meta })
```

- [ ] **Step 4: Correr para verificar que pasa**

Run: `npx vitest run test/contenido.test.ts`
Expected: PASS, 5 tests.

- [ ] **Step 5: Probar que las cuatro reglas del texto son reales, una por una**

Un test que declara cuatro rechazos puede estar pasando por un solo motivo. Comprobalo: comentá **una** de las tres `.refine` de `reglasDeTexto` (y para el largo, subí el `max` a 999), corré, y confirmá que el test se pone **rojo por ese caso y no por otro**. Restaurá y pasá a la siguiente.

Run (cuatro veces, una por regla): `npx vitest run test/contenido.test.ts -t 'rechaza vacío'`
Expected: cada vez, FAIL señalando exactamente el caso que quedó sin cubrir.

Pegá en el reporte las cuatro salidas. Si alguna comentada **no** pone el test en rojo, esa regla no está haciendo nada: paralo y decilo.

- [ ] **Step 6: El guard de la trampa del registro**

Agregá a `test/contenido.test.ts`:

```ts
  it('anotar no contamina: dos campos derivados de la misma base no comparten etiqueta', () => {
    // `registry.get()` sigue la cadena de padres. Si alguien anotara una
    // base compartida en vez del esquema terminado, TODOS los campos
    // derivados de ella heredarían esa etiqueta y el panel mostraría el
    // mismo nombre en veinte lugares. Verificado contra zod 4.4.3.
    const a = texto({ etiqueta: 'Uno', seccion: 'portada', ayuda: 'a', max: 10 })
    const b = texto({ etiqueta: 'Dos', seccion: 'pie', ayuda: 'b', max: 60 })
    expect(panel.get(a)?.etiqueta).toBe('Uno')
    expect(panel.get(b)?.etiqueta).toBe('Dos')
    expect(panel.get(a)?.max).toBe(10)
    expect(panel.get(b)?.max).toBe(60)
  })
```

Run: `npx vitest run test/contenido.test.ts`
Expected: PASS, 6 tests.

- [ ] **Step 7: Correr la suite completa y commitear**

Run: `pnpm test && pnpm typecheck`
Expected: PASS — 689 tests, typecheck en 0 errores.

```bash
git add src/contenido/campos.ts test/contenido.test.ts
git commit -m "feat: MetaCampo y los constructores de texto — un campo sin etiqueta no compila

El panel se va a pintar leyendo este registro, así que la etiqueta en
español y la oración de «dónde vive» son parte del tipo, no documentación
opcional. Agregar un campo al sitio y olvidarse de explicárselo a la
clienta deja de ser posible.

`max` es techo de cordura, NO límite de diseño: si el texto entra o no lo
decide el medidor midiendo la página de verdad, porque 31 «W» miden 426 px
y 31 «i» miden 142 px. Un número escrito a mano es a la vez falso positivo
y falso negativo.

Tres cosas verificadas contra zod 4.4.3: el registro existe, .refine()
devuelve objeto nuevo sin envolver el esquema, y registry.get() sigue la
cadena de padres — por eso anota() recibe el esquema terminado y hay un
guard que lo fija.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 3: Los constructores de dato, identificador y forma

Lo que falta de `campos.ts`. Son mecánicos comparados con los de texto, pero dos traen reglas que hoy no existen en ningún lado: el precio como entero acotado y el `tupla` de forma fija.

**Files:**
- Modify: `src/contenido/campos.ts`, `test/contenido.test.ts`

**Interfaces:**
- Produces: `precio`, `precioONada`, `numero`, `claveSabor`, `tokenColor`, `ruta`, `url`, `correo`, `slug`, `archivo`, `derivado`, `grupo`, `tupla`, `lista`.

**La distinción que importa:** `tupla` es forma **fija** —los 3 renglones del titular, las 2 líneas de dirección, las 2 opciones del formulario— y el panel dibuja cajas fijas, sin botón de «agregar». `lista` es colección con `min`/`max`: agregar, quitar, reordenar.

- [ ] **Step 1: Escribir los tests que fallan**

Agregá a `test/contenido.test.ts`:

```ts
  it('el precio es entero y acotado: ni string, ni decimal, ni cero, ni absurdo', () => {
    const p = precio({ etiqueta: 'Precio de la barra', seccion: 'sabores', ayuda: 'x' })
    expect(p.safeParse(108).success).toBe(true)
    // «$108» como string es el error que rompe tres tests del sitio.
    expect(p.safeParse('108').success).toBe(false)
    expect(p.safeParse(108.5).success).toBe(false)
    expect(p.safeParse(0).success).toBe(false)
    expect(p.safeParse(-1).success).toBe(false)
    expect(p.safeParse(100000).success).toBe(false)
  })

  it('la tupla es forma fija: no acepta ni uno de más ni uno de menos', () => {
    const t = tupla({
      etiqueta: 'Titular', seccion: 'portada', ayuda: 'Las tres líneas del título grande.',
      partes: [
        texto({ etiqueta: 'Renglón 1', seccion: 'portada', ayuda: 'a', max: 40 }),
        texto({ etiqueta: 'Renglón 2', seccion: 'portada', ayuda: 'b', max: 40 }),
        texto({ etiqueta: 'Renglón 3', seccion: 'portada', ayuda: 'c', max: 40 }),
      ],
    })
    expect(t.safeParse(['CHOCOLATE', 'MEXICANO,', '70% CACAO.']).success).toBe(true)
    expect(t.safeParse(['CHOCOLATE', 'MEXICANO,']).success).toBe(false)
    expect(t.safeParse(['A', 'B', 'C', 'D']).success).toBe(false)
  })

  it('la lista respeta su mínimo y su máximo — los conteos del sitio dependen de eso', () => {
    const l = lista({
      etiqueta: 'Preguntas frecuentes', seccion: 'preguntas', ayuda: 'x',
      de: texto({ etiqueta: 'Pregunta', seccion: 'preguntas', ayuda: 'y', max: 90 }),
      min: 4, max: 12,
    })
    expect(l.safeParse(['a', 'b', 'c', 'd']).success).toBe(true)
    expect(l.safeParse(['a', 'b', 'c']).success).toBe(false)
    expect(l.safeParse(Array(13).fill('a')).success).toBe(false)
  })

  it('claveSabor solo acepta claves que existen en los tokens de color', () => {
    const c = claveSabor({ etiqueta: 'Sabor', seccion: 'sabores', ayuda: 'x' })
    expect(c.safeParse('canela').success).toBe(true)
    expect(c.safeParse('inventado').success).toBe(false)
  })
```

Sumá al import: `precio, tupla, lista, claveSabor`.

- [ ] **Step 2: Correr para verificar que falla**

Run: `npx vitest run test/contenido.test.ts`
Expected: FAIL — los cuatro constructores no existen.

- [ ] **Step 3: Escribirlos**

Sumá el import arriba del archivo, junto al de `zod`:

```ts
import { sabor } from '../tokens/color'
```

Y agregá los constructores al final de `src/contenido/campos.ts`:

```ts

/**
 * Un precio en pesos. ENTERO: `precioMXN` lo formatea, y un decimal acá
 * sale como «$108.5» en la página. El techo de 99.999 no es capricho —
 * un cero de más es un número perfectamente válido y ninguna otra regla
 * lo ataja (la bandeja de publicar además avisa por saltos grandes).
 */
export const precio = (meta: Base) =>
  anota(z.int().min(1).max(99_999), { control: 'precio', ...meta })

/** Un precio que puede no existir todavía (el polvo dice «Próximamente»). */
export const precioONada = (meta: Base) =>
  anota(z.int().min(1).max(99_999).nullable(), { control: 'precio', ...meta })

/** Un número que no es plata (el `orden` impreso de la serie). */
export const numero = (meta: Base & { min: number; max: number }) =>
  anota(z.int().min(meta.min).max(meta.max), { control: 'oculto', quien: 'marcos', ...meta })

/**
 * Una clave del token `sabor`. No es texto: nombra el color de la banda,
 * la tinta medida y seis archivos de imagen. La clienta no la ve.
 */
export const claveSabor = (meta: Base) =>
  anota(
    z.string().refine((v) => v in sabor, 'No es un sabor del sistema de color.'),
    { control: 'oculto', quien: 'marcos', ...meta },
  )

/** Igual que claveSabor pero para cualquier token de color declarado. */
export const tokenColor = (meta: Base & { validos: readonly string[] }) =>
  anota(
    z.string().refine((v) => meta.validos.includes(v), 'No es un token de color del sistema.'),
    { control: 'oculto', quien: 'marcos', ...meta },
  )

/** Una ruta interna: '/fichas-tecnicas'. */
export const ruta = (meta: Base) =>
  anota(z.string().regex(/^\/[\w\-/]*$/, 'Tiene que empezar con «/».'), {
    control: 'oculto', quien: 'marcos', ...meta,
  })

/** Una dirección web completa. */
export const url = (meta: Base) =>
  anota(z.url(), { control: 'oculto', quien: 'marcos', ...meta })

/** Un correo. Lo edita la clienta: es el que aparece en el pie. */
export const correo = (meta: Base) =>
  anota(z.email(), { control: 'texto', ...meta })

/** El identificador de un sabor. INMUTABLE: nombra seis archivos. */
export const slug = (meta: Base) =>
  anota(z.string().regex(/^[a-z0-9-]+$/, 'Solo minúsculas, números y guiones.'), {
    control: 'oculto', quien: 'marcos', ...meta,
  })

/** El nombre de archivo de un PDF de ficha, sin extensión. */
export const archivo = (meta: Base) =>
  anota(z.string().regex(/^[a-z0-9-]+$/, 'Solo minúsculas, números y guiones.'), {
    control: 'oculto', quien: 'marcos', ...meta,
  })

/**
 * Un valor que NO se edita porque se calcula (ver derivados.ts). Vive en
 * el esquema para que el panel sepa dibujarlo en gris con su explicación,
 * pero no sale del JSON.
 */
export const derivado = (meta: Base & { de: string }) =>
  anota(z.int().min(1).max(99_999), { control: 'derivado', quien: 'marcos', ...meta })

/** Un bloque con campos nombrados. */
export const grupo = <T extends z.ZodRawShape>(meta: Base & { campos: T }) =>
  anota(z.object(meta.campos), { control: 'oculto', ...meta })

/**
 * Forma FIJA: los 3 renglones del titular, las 2 líneas de la dirección.
 * El panel dibuja cajas fijas, sin «agregar» ni «quitar» — porque la
 * plantilla las indexa por posición y un cuarto renglón se pierde en
 * silencio.
 */
export const tupla = <T extends readonly [z.ZodType, ...z.ZodType[]]>(
  meta: Base & { partes: T },
) => anota(z.tuple(meta.partes), { control: 'renglones', ...meta })

/** Colección con mínimo y máximo: agregar, quitar, reordenar. */
export const lista = <T extends z.ZodType>(meta: Base & { de: T; min: number; max: number }) =>
  anota(z.array(meta.de).min(meta.min).max(meta.max), { control: 'lista', ...meta })
```

- [ ] **Step 4: Correr para verificar que pasa**

Run: `npx vitest run test/contenido.test.ts`
Expected: PASS, 10 tests.

- [ ] **Step 5: Probar que el guard del precio entero es real**

Cambiá temporalmente `z.int()` por `z.number()` en `precio`.

Run: `npx vitest run test/contenido.test.ts -t 'el precio es entero'`
Expected: **FAIL** — `expected false to be true` en el caso `108.5`.

Restaurá. Pegá las dos salidas en el reporte.

- [ ] **Step 6: Probar que la tupla no es un array disfrazado**

Cambiá temporalmente `z.tuple(meta.partes)` por `z.array(meta.partes[0])`.

Run: `npx vitest run test/contenido.test.ts -t 'la tupla es forma fija'`
Expected: **FAIL** — acepta 2 y acepta 4 elementos. Es exactamente el bug que dejaría perder el tercer renglón del titular en silencio.

Restaurá. Pegá las dos salidas.

- [ ] **Step 7: Correr la suite y commitear**

Run: `pnpm test && pnpm typecheck`
Expected: PASS — 693 tests, 0 errores.

```bash
git add src/contenido/campos.ts test/contenido.test.ts
git commit -m "feat: los constructores de dato, identificador y forma

El precio es entero acotado 1–99.999: un decimal sale como «\$108.5» en la
página y un string es el error que hoy rompe tres tests del sitio.

`tupla` y `lista` no son lo mismo y el panel los dibuja distinto: la tupla
es forma fija —los 3 renglones del titular, las 2 líneas de dirección— y
la plantilla las indexa por posición, así que un cuarto renglón se perdería
en silencio. La lista sí tiene agregar, quitar y reordenar.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 4: `recorre()` y el canario de los internals de Zod

`recorre()` es lo único del sistema que toca interna de Zod. Si Zod mueve una forma, tiene que fallar acá —con un mensaje que lo diga— y no adentro del panel seis meses después.

**Files:**
- Create: `src/contenido/carga.ts`
- Modify: `test/contenido.test.ts`

**Interfaces:**
- Produces: `recorre(esquema, visita)` — recorre el árbol del esquema emitiendo la ruta punteada de cada hoja y su metadato. Lo consumen `serializa()` (Tarea 5), `validacion.ts` (Tarea 8) y, en la Fase 6, el panel para pintarse.

**Las cinco formas, verificadas contra zod 4.4.3** — no las re-investigues, pero el canario las afirma:

| Tipo | `_zod.def` trae |
|---|---|
| object | `shape` |
| array | `element` |
| tuple | `items` + `rest` |
| optional | `innerType` |
| discriminatedUnion | `options` + `discriminator` + `inclusive` |

- [ ] **Step 1: Escribir el canario y el test del recorrido**

Agregá a `test/contenido.test.ts`:

```ts
  it('canario: las formas internas de Zod son las que recorre() supone', () => {
    // recorre() es lo ÚNICO que toca interna de Zod. Si una versión nueva
    // mueve estas claves, el recorrido devolvería rutas vacías y el panel
    // se pintaría sin campos, sin un solo error. Este test es el lugar
    // donde eso tiene que doler, y el mensaje dice qué hacer.
    const def = (e: unknown) => Object.keys((e as { _zod: { def: object } })._zod.def)
    expect(def(z.object({ a: z.string() }))).toContain('shape')
    expect(def(z.array(z.string()))).toContain('element')
    expect(def(z.tuple([z.string(), z.string()]))).toContain('items')
    expect(def(z.string().optional())).toContain('innerType')
    const du = z.discriminatedUnion('t', [
      z.object({ t: z.literal('a') }),
      z.object({ t: z.literal('b') }),
    ])
    expect(def(du)).toEqual(expect.arrayContaining(['options', 'discriminator']))
  })

  it('recorre() emite la ruta punteada de cada hoja, con su metadato', () => {
    const esquema = grupo({
      etiqueta: 'Portada', seccion: 'portada', ayuda: 'x',
      campos: {
        titular: tupla({
          etiqueta: 'Titular', seccion: 'portada', ayuda: 'y',
          partes: [
            texto({ etiqueta: 'Renglón 1', seccion: 'portada', ayuda: 'a', max: 40 }),
            texto({ etiqueta: 'Renglón 2', seccion: 'portada', ayuda: 'b', max: 40 }),
          ],
        }),
        sub: texto({ etiqueta: 'Bajada', seccion: 'portada', ayuda: 'c', max: 200 }),
      },
    })

    const hojas: string[] = []
    recorre(esquema, (ruta, meta) => hojas.push(`${ruta}=${meta?.etiqueta ?? '-'}`))

    expect(hojas).toEqual([
      'titular.0=Renglón 1',
      'titular.1=Renglón 2',
      'sub=Bajada',
    ])
  })
```

Sumá al import: `grupo, recorre` y `import { z } from 'zod'`.

- [ ] **Step 2: Correr para verificar que falla**

Run: `npx vitest run test/contenido.test.ts`
Expected: FAIL — `recorre` no existe.

- [ ] **Step 3: Escribir `recorre()`**

Create `src/contenido/carga.ts`:

```ts
/*
 * El cargador de contenido.
 *
 * `recorre()` es lo ÚNICO del sistema que toca interna de Zod. Está
 * acotado a propósito a ~60 líneas y a cinco formas, y `test/contenido.test.ts`
 * afirma cada una: si una versión nueva de Zod las mueve, falla ahí —con
 * un mensaje que lo explica— y no adentro del panel seis meses después.
 */
import { z } from 'zod'
import { panel, type MetaCampo } from './campos'

type Def = { type: string; [clave: string]: unknown }
const definicion = (e: unknown) => (e as { _zod: { def: Def } })._zod.def

/**
 * Recorre el árbol del esquema y llama a `visita` en cada HOJA, con su
 * ruta punteada ('hero.titular.1') y el metadato del panel.
 *
 * Los contenedores (object, array, tuple) no son hojas: se atraviesan.
 * Un `optional` se desenvuelve y la hoja es lo de adentro.
 */
export function recorre(
  esquema: z.ZodType,
  visita: (ruta: string, meta: MetaCampo | undefined, hoja: z.ZodType) => void,
  prefijo = '',
): void {
  const def = definicion(esquema)
  const con = (parte: string) => (prefijo ? `${prefijo}.${parte}` : parte)

  switch (def.type) {
    case 'optional':
      recorre(def.innerType as z.ZodType, visita, prefijo)
      return
    case 'object': {
      const shape = def.shape as Record<string, z.ZodType>
      for (const clave of Object.keys(shape)) recorre(shape[clave], visita, con(clave))
      return
    }
    case 'tuple': {
      const items = def.items as z.ZodType[]
      items.forEach((item, i) => recorre(item, visita, con(String(i))))
      return
    }
    case 'array':
      // La ruta del elemento lleva `[]`: el panel la instancia por índice
      // cuando pinta la lista, y el dato real dice cuántos hay.
      recorre(def.element as z.ZodType, visita, `${prefijo}[]`)
      return
    default:
      visita(prefijo, panel.get(esquema), esquema)
  }
}
```

- [ ] **Step 4: Correr para verificar que pasa**

Run: `npx vitest run test/contenido.test.ts`
Expected: PASS, 12 tests.

- [ ] **Step 5: Probar que el canario canta**

Cambiá temporalmente en `recorre()` el `case 'object'` para que lea `def.forma` en vez de `def.shape`.

Run: `npx vitest run test/contenido.test.ts -t 'recorre'`
Expected: **FAIL**. El mecanismo exacto es un `TypeError: Cannot convert undefined or null to object` —`Object.keys(undefined)` tira— y no un `[]` silencioso; lo que importa es que el test cae. En el panel, donde el recorrido corre sobre esquemas de forma variada, el mismo desajuste puede degradar a hojas vacías sin excepción: por eso el canario afirma las formas, en vez de confiar en que siempre truene.

Restaurá. Pegá las dos salidas.

- [ ] **Step 6: Correr la suite y commitear**

Run: `pnpm test && pnpm typecheck`
Expected: PASS — 695 tests, 0 errores.

```bash
git add src/contenido/carga.ts test/contenido.test.ts
git commit -m "feat: recorre() y el canario de los internals de Zod

recorre() es lo único del sistema que toca interna de Zod: cinco formas,
~60 líneas, y un test que afirma cada una. Si una versión nueva las mueve,
el recorrido devolvería rutas vacías y el panel se pintaría sin campos SIN
UN SOLO ERROR. El canario hace que eso duela acá y no allá.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 5: `cargar()` y `serializa()`

Los dos extremos del archivo. `cargar()` es lo que hace que un JSON inválido reviente `astro build` —y que Vercel deje servido el deploy anterior— en vez de publicar una página rota. `serializa()` produce bytes canónicos y, de yapa, es la prueba de que el esquema describe exactamente lo que hay.

**Files:**
- Modify: `src/contenido/carga.ts`, `test/contenido.test.ts`

**Interfaces:**
- Consumes: `recorre()` (Tarea 4).
- Produces: `cargar(archivo, esquema, crudo)` y `serializa(esquema, valor)`. Los consumen las tres fachadas y el script de migración (Parte B).

- [ ] **Step 1: Escribir los tests que fallan**

```ts
  it('cargar() tira con la ruta punteada y el mensaje en español', () => {
    const esquema = grupo({
      etiqueta: 'x', seccion: 'portada', ayuda: 'x',
      campos: { titulo: texto({ etiqueta: 'Título', seccion: 'portada', ayuda: 'y', max: 20 }) },
    })
    expect(() => cargar('datos/prueba.json', esquema, { titulo: '   ' })).toThrow(
      /datos\/prueba\.json.*titulo.*vacío/s,
    )
  })

  it('cargar() congela: el contenido no se muta por accidente en runtime', () => {
    const esquema = grupo({
      etiqueta: 'x', seccion: 'portada', ayuda: 'x',
      campos: { titulo: texto({ etiqueta: 'Título', seccion: 'portada', ayuda: 'y', max: 20 }) },
    })
    const doc = cargar('datos/prueba.json', esquema, { titulo: 'Hola' }) as { titulo: string }
    expect(Object.isFrozen(doc)).toBe(true)
    expect(() => { (doc as { titulo: string }).titulo = 'otro' }).toThrow()
  })

  it('serializa() escribe bytes canónicos: orden del esquema e invisibles escapados', () => {
    const esquema = grupo({
      etiqueta: 'x', seccion: 'productos', ayuda: 'x',
      campos: {
        peso: medida({ etiqueta: 'Peso', seccion: 'productos', ayuda: 'y', max: 20 }),
        nombre: texto({ etiqueta: 'Nombre', seccion: 'productos', ayuda: 'z', max: 20 }),
      },
    })
    // Se pasa con las claves al REVÉS del esquema a propósito.
    const bytes = serializa(esquema, { nombre: 'Gotas', peso: '250 g' })
    // El orden lo manda el esquema, no el objeto: si no, dos guardados
    // seguidos producen diffs distintos sin que cambie nada.
    expect(bytes.indexOf('"peso"')).toBeLessThan(bytes.indexOf('"nombre"'))
    // El espacio duro va escapado: en el fuente no hay ni uno literal, y
    // así se ve en el diff que está.
    expect(bytes).toContain('250\\u00a0g')
    expect(bytes).not.toContain('250 g')
  })

  it('serializa() tira si el esquema y el dato no dicen lo mismo', () => {
    // Es la prueba de completitud de la migración: si el esquema declara
    // una ruta que el objeto no tiene, o el objeto trae una que el
    // esquema no declara, no se escribe nada.
    const esquema = grupo({
      etiqueta: 'x', seccion: 'portada', ayuda: 'x',
      campos: { a: texto({ etiqueta: 'A', seccion: 'portada', ayuda: 'y', max: 9 }) },
    })
    expect(() => serializa(esquema, {})).toThrow(/a/)
    expect(() => serializa(esquema, { a: 'ok', sobra: 1 })).toThrow(/sobra/)
  })
```

Sumá al import: `cargar, serializa, medida`.

- [ ] **Step 2: Correr para verificar que falla**

Run: `npx vitest run test/contenido.test.ts`
Expected: FAIL — `cargar` y `serializa` no existen.

- [ ] **Step 3: Escribirlos**

Agregá a `src/contenido/carga.ts`:

```ts
/** Congela hacia adentro. La garantía real es de runtime, no de tipos. */
function congela<T>(valor: T): T {
  if (valor === null || typeof valor !== 'object') return valor
  for (const v of Object.values(valor)) congela(v)
  return Object.freeze(valor)
}

/**
 * Valida el JSON contra su esquema y devuelve el objeto congelado.
 *
 * Si falla, TIRA. Y tiene que tirar: `cargar()` corre adentro del módulo
 * que `index.astro` importa, así que un JSON inválido revienta
 * `astro build` y Vercel deja servido el deploy anterior. Envolverlo en
 * try/catch publicaría la página rota, que es exactamente lo contrario.
 */
export function cargar<E extends z.ZodType>(
  archivo: string,
  esquema: E,
  crudo: unknown,
): Readonly<z.infer<E>> {
  const r = esquema.safeParse(crudo)
  if (!r.success) {
    const problemas = r.error.issues
      .map((i) => `  ${i.path.join('.') || '(raíz)'}: ${i.message}`)
      .join('\n')
    throw new Error(`${archivo} — ${r.error.issues.length} problema(s):\n${problemas}`)
  }
  return congela(r.data) as Readonly<z.infer<E>>
}

/**
 * Bytes canónicos: el orden de las claves lo manda el ESQUEMA, no el
 * objeto, y los invisibles (U+00A0 y compañía) salen escapados.
 *
 * Lo primero evita que dos guardados seguidos produzcan diffs distintos
 * sin que haya cambiado nada. Lo segundo conserva una convención que el
 * repo ya tiene: hoy hay cero caracteres U+00A0 literales en el fuente.
 *
 * Tira si el esquema y el dato no coinciden — y por eso además sirve como
 * prueba de que el esquema describe exactamente el contenido de hoy.
 */
export function serializa<E extends z.ZodType>(esquema: E, valor: unknown): string {
  const ordenado = ordenaSegun(esquema, valor, '')
  return JSON.stringify(ordenado, null, 2).replace(
    // Escritos como \u para que el archivo no dependa de caracteres que
    // un copiar-y-pegar puede comerse: espacio duro, los espacios finos,
    // los de ancho cero y el BOM.
    /[\u00a0\u2000-\u200d\ufeff]/g,
    (c) => `\\u${c.charCodeAt(0).toString(16).padStart(4, '0')}`,
  )
}

function ordenaSegun(esquema: z.ZodType, valor: unknown, ruta: string): unknown {
  const def = definicion(esquema)
  const donde = ruta || '(raíz)'

  switch (def.type) {
    case 'optional':
      return valor === undefined ? undefined : ordenaSegun(def.innerType as z.ZodType, valor, ruta)

    case 'object': {
      const shape = def.shape as Record<string, z.ZodType>
      if (valor === null || typeof valor !== 'object' || Array.isArray(valor)) {
        throw new Error(`${donde}: el esquema espera un bloque y el dato trae ${typeof valor}.`)
      }
      const dato = valor as Record<string, unknown>
      const sobrantes = Object.keys(dato).filter((k) => !(k in shape))
      if (sobrantes.length) {
        throw new Error(`${donde}: el dato trae claves que el esquema no declara: ${sobrantes.join(', ')}.`)
      }
      const salida: Record<string, unknown> = {}
      for (const clave of Object.keys(shape)) {
        const hijo = shape[clave]
        const opcional = definicion(hijo).type === 'optional'
        if (!(clave in dato)) {
          if (opcional) continue
          throw new Error(`${donde}: falta «${clave}», que el esquema declara.`)
        }
        salida[clave] = ordenaSegun(hijo, dato[clave], ruta ? `${ruta}.${clave}` : clave)
      }
      return salida
    }

    case 'tuple': {
      const items = def.items as z.ZodType[]
      if (!Array.isArray(valor) || valor.length !== items.length) {
        throw new Error(`${donde}: el esquema espera exactamente ${items.length} elementos.`)
      }
      return items.map((it, i) => ordenaSegun(it, valor[i], `${ruta}.${i}`))
    }

    case 'array': {
      if (!Array.isArray(valor)) throw new Error(`${donde}: el esquema espera una lista.`)
      return valor.map((v, i) => ordenaSegun(def.element as z.ZodType, v, `${ruta}.${i}`))
    }

    default:
      return valor
  }
}
```

- [ ] **Step 4: Correr para verificar que pasa**

Run: `npx vitest run test/contenido.test.ts`
Expected: PASS, 16 tests.

- [ ] **Step 5: Probar el ida y vuelta de bytes, que es lo que hace canónico a canónico**

Agregá:

```ts
  it('serializa(cargar(bytes)) devuelve los mismos bytes', () => {
    const esquema = grupo({
      etiqueta: 'x', seccion: 'productos', ayuda: 'x',
      campos: {
        peso: medida({ etiqueta: 'Peso', seccion: 'productos', ayuda: 'y', max: 20 }),
        nombre: texto({ etiqueta: 'Nombre', seccion: 'productos', ayuda: 'z', max: 20 }),
      },
    })
    const bytes = serializa(esquema, { peso: '250 g', nombre: 'Gotas' })
    const vuelta = serializa(esquema, cargar('x.json', esquema, JSON.parse(bytes)))
    expect(vuelta).toBe(bytes)
  })
```

Run: `npx vitest run test/contenido.test.ts`
Expected: PASS, 17 tests.

- [ ] **Step 6: Probar que el orden lo manda el esquema y no el objeto**

Cambiá temporalmente el `case 'object'` de `ordenaSegun` para que itere `Object.keys(dato)` en vez de `Object.keys(shape)`.

Run: `npx vitest run test/contenido.test.ts -t 'bytes canónicos'`
Expected: **FAIL** — `"peso"` queda después de `"nombre"`, porque el objeto de prueba se pasa al revés a propósito.

Restaurá. Pegá las dos salidas.

- [ ] **Step 7: Probar que `cargar()` no se puede envolver en try/catch sin que se note**

Verificá a mano que un JSON inválido rompe el build de verdad — es la promesa central del cargador:

```bash
npx tsx -e "
import { z } from 'zod'
import { cargar } from './src/contenido/carga'
import { grupo, texto } from './src/contenido/campos'
const e = grupo({etiqueta:'x',seccion:'portada',ayuda:'x',campos:{t:texto({etiqueta:'T',seccion:'portada',ayuda:'y',max:5})}})
try { cargar('datos/roto.json', e, {t:''}) } catch (err) { console.log(String(err).split('\n').slice(0,3).join('\n')) }
"
```
Expected: el mensaje con la ruta del archivo, el conteo de problemas y `t: No puede quedar vacío.` Pegalo en el reporte.

- [ ] **Step 8: Correr la suite y commitear**

Run: `pnpm test && pnpm typecheck`
Expected: PASS — 700 tests, 0 errores.

```bash
git add src/contenido/carga.ts test/contenido.test.ts
git commit -m "feat: cargar() y serializa() — los dos extremos de la capa de contenido

cargar() valida, congela y TIRA con la ruta punteada en español. Tiene que
tirar: corre adentro del módulo que index.astro importa, así que un JSON
inválido revienta astro build y Vercel deja servido el deploy anterior.
Envolverlo en try/catch publicaría la página rota.

serializa() escribe bytes canónicos —el orden lo manda el esquema, no el
objeto— para que dos guardados seguidos no produzcan diffs distintos sin
que cambie nada, y escapa los invisibles conservando una convención que el
repo ya tiene. Y tira si el esquema y el dato no coinciden, que de yapa es
la prueba de completitud que va a usar la migración.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 6: `conteos.ts` y `color-sabor.ts`

Dos módulos puros y chicos. El primero resuelve el «15» escrito en nueve lugares; el segundo, la regla de contraste que hoy vive a mano en los tokens.

**Files:**
- Create: `src/contenido/conteos.ts`, `src/contenido/color-sabor.ts`
- Modify: `test/contenido.test.ts`

**Interfaces:**
- Produces: `enLetras(n)`, `cruzaConteo(texto, cuenta, cantidad)` desde `./conteos`; `resuelveColor(clave)`, `mejorTinta(fondo)`, `contrasteSuficiente(fondo, tinta, slug)` desde `./color-sabor`.

**El problema del «15»:** hoy la cifra está en `anaquel.kicker` («LOS 15 SABORES»), en `gotas.sabores` («6 sabores»), en textos en prosa de negocios y en el contador del anaquel. Si la clienta agrega un sabor (Fase 7), esos textos mienten y **ningún test lo detecta**. La regla `cuenta` del metadato cruza el texto contra la lista real, en cifra **y** en letras — porque «seis sabores» es tan probable como «6 sabores».

**La excepción de contraste:** `src/tokens/color.ts:182` declara `saboresSoloDisplay = ['hierbabuena']`, medido en 4.41. La regla del esquema **hereda esa excepción por slug**, no la reinventa ni la borra.

- [ ] **Step 1: Escribir los tests que fallan**

```ts
  it('cruzaConteo detecta la cifra desactualizada, en número y en letras', () => {
    expect(cruzaConteo('LOS 15 SABORES', 15)).toBeNull()
    expect(cruzaConteo('LOS 15 SABORES', 16)).toMatch(/15.*16/)
    // «seis sabores» es tan probable como «6 sabores» y hoy nada lo mira.
    expect(cruzaConteo('Seis sabores de gotas', 6)).toBeNull()
    expect(cruzaConteo('Seis sabores de gotas', 7)).toMatch(/seis.*7/i)
    // Un número que no es el conteo no molesta.
    expect(cruzaConteo('70% cacao, 15 sabores', 15)).toBeNull()
  })

  it('la regla de contraste hereda la excepción declarada en los tokens', () => {
    // Un par nuevo tiene que dar 4.5 o más.
    expect(contrasteSuficiente('#7D0303', '#FFFFFF', 'canela')).toBe(true)
    expect(contrasteSuficiente('#F8ECDE', '#FFFFFF', 'canela')).toBe(false)
    // Hierbabuena da 4.41 medido y está declarada soloDisplay en
    // src/tokens/color.ts:182. La regla NO la reinventa ni la borra:
    // la hereda por slug.
    const { sabor, tintaSabor } = tokens
    expect(contrasteSuficiente(sabor.hierbabuena, tintaSabor.hierbabuena, 'hierbabuena')).toBe(true)
    // Y el mismo par, con otro slug, sigue siendo insuficiente.
    expect(contrasteSuficiente(sabor.hierbabuena, tintaSabor.hierbabuena, 'canela')).toBe(false)
  })
```

Sumá `import * as tokens from '../src/tokens/color'` y al import de contenido: `cruzaConteo, contrasteSuficiente`.

- [ ] **Step 2: Correr para verificar que falla**

Run: `npx vitest run test/contenido.test.ts`
Expected: FAIL — los módulos no existen.

- [ ] **Step 3: Escribir `conteos.ts`**

```ts
/*
 * El «15» está escrito en nueve lugares del sitio: el kicker del anaquel,
 * el contador, textos en prosa de negocios, «6 sabores» de las gotas. Si
 * la clienta agrega un sabor, todos esos textos mienten y hoy no hay un
 * solo test que lo detecte.
 *
 * La regla `cuenta` del metadato cruza el texto contra la lista real. En
 * cifra Y en letras, porque «seis sabores» es tan probable como «6».
 */
const LETRAS = [
  'cero', 'uno', 'dos', 'tres', 'cuatro', 'cinco', 'seis', 'siete', 'ocho',
  'nueve', 'diez', 'once', 'doce', 'trece', 'catorce', 'quince', 'dieciséis',
  'diecisiete', 'dieciocho', 'diecinueve', 'veinte',
] as const

export const enLetras = (n: number): string => LETRAS[n] ?? String(n)

/**
 * Devuelve el aviso si el texto menciona una cantidad distinta de la real,
 * o null si está bien. `esperado` es cuántos hay de verdad.
 */
export function cruzaConteo(texto: string, esperado: number): string | null {
  for (let n = 0; n <= 20; n++) {
    if (n === esperado) continue
    const cifra = new RegExp(`(^|[^\\d])${n}([^\\d]|$)`)
    const letra = new RegExp(`(^|[^a-záéíóúñ])${enLetras(n)}([^a-záéíóúñ]|$)`, 'i')
    if (cifra.test(texto)) return `dice «${n}» pero hoy hay ${esperado}.`
    if (letra.test(texto)) return `dice «${enLetras(n)}» pero hoy hay ${esperado}.`
  }
  return null
}
```

- [ ] **Step 4: Escribir `color-sabor.ts`**

```ts
/*
 * La regla de contraste, en un solo lugar.
 *
 * Hoy vive repartida: `contrastRatio` en los tokens y una lista de
 * excepciones (`saboresSoloDisplay`) al lado. Cuando la clienta pueda dar
 * de alta un sabor (fase 7), el color nuevo va a tener que pasar por acá.
 */
import { contrastRatio } from '../tokens/contrast'
import { sabor, tintaSabor, saboresSoloDisplay } from '../tokens/color'

export const resuelveColor = (clave: string): string | undefined =>
  (sabor as Record<string, string>)[clave]

/** La tinta declarada para ese sabor. */
export const mejorTinta = (clave: string): string | undefined =>
  (tintaSabor as Record<string, string>)[clave]

/**
 * ¿Se puede poner texto normal sobre esa banda?
 *
 * El umbral es 4.5 (AA). La excepción es por SLUG y sale de
 * `saboresSoloDisplay` en los tokens: hierbabuena da 4.41 medido y está
 * declarada ahí. La regla la hereda; no la reinventa ni la pisa.
 */
export function contrasteSuficiente(fondo: string, tinta: string, slug: string): boolean {
  if ((saboresSoloDisplay as readonly string[]).includes(slug)) return true
  return contrastRatio(fondo, tinta) >= 4.5
}
```

- [ ] **Step 5: Correr para verificar que pasa**

Run: `npx vitest run test/contenido.test.ts`
Expected: PASS, 19 tests.

- [ ] **Step 6: Probar que la mitad «en letras» del conteo es real**

Cambiá temporalmente `cruzaConteo` para que solo pruebe la cifra (borrá las dos líneas del `letra`).

Run: `npx vitest run test/contenido.test.ts -t 'cruzaConteo'`
Expected: **FAIL** — `expected null to match /seis.*7/i`. Sin esa mitad, un «Seis sabores» desactualizado pasaría de largo.

Restaurá. Pegá las dos salidas.

- [ ] **Step 7: Probar que la excepción de contraste se hereda y no se inventa**

Cambiá temporalmente `contrasteSuficiente` para que ignore `saboresSoloDisplay` (borrá la línea del `if`).

Run: `npx vitest run test/contenido.test.ts -t 'contraste'`
Expected: **FAIL** en el caso de hierbabuena. Confirma que el par real da menos de 4.5 y que la excepción es lo único que lo salva — o sea que el test no está pasando por casualidad.

Restaurá. Pegá las dos salidas.

- [ ] **Step 8: Correr la suite y commitear**

Run: `pnpm test && pnpm typecheck`
Expected: PASS — 702 tests, 0 errores.

```bash
git add src/contenido/conteos.ts src/contenido/color-sabor.ts test/contenido.test.ts
git commit -m "feat: la regla de conteo y la de contraste, en un solo lugar cada una

El «15» está escrito en nueve lugares del sitio y hoy ningún test detecta
que mientan cuando cambie la lista. cruzaConteo() los cruza contra el dato
real, en cifra Y en letras — «seis sabores» es tan probable como «6».

La regla de contraste hereda por slug la excepción que ya está declarada en
los tokens (hierbabuena, 4.41 medido): no la reinventa ni la pisa. Cuando
la clienta pueda dar de alta un sabor, el color nuevo pasa por acá.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 7: `derivados.ts` — lo que la clienta no edita porque se calcula

**Files:**
- Create: `src/contenido/derivados.ts`
- Modify: `test/contenido.test.ts`

**Interfaces:**
- Produces: `precioDesde(lista)`, `precioDe(lista, clave)`. Los consumen las fachadas en la Parte B.

**El problema, medido en el repo de hoy:** el mismo precio está escrito en más lugares de los que nadie contó.

| Precio | Dónde está escrito |
|---|---|
| 108 | `sabores[].precio` (14 barras) **y** `negocios.tabs[2].precio` |
| 258 | `gotas[].precio` (5 de 6) **y** `marca.gotas.precioDesde` **y** `tabs[1].precio` |
| 340 | `gotas[0].precio` **y** `marca.gotas.precioJengibre` |
| 118 | `marca.minis.precio` (y `paqueteSeis.precio`, ya borrado en la Fase 0) |

Si la clienta sube las barras a 130 desde el anaquel, la pestaña «Para negocios» le sigue diciendo «desde $108» a las cafeterías. Pedirle que mantenga sincronizados a mano tres números que la máquina puede calcular es pedirle que se equivoque.

- [ ] **Step 1: Escribir los tests que fallan**

```ts
  it('los precios repetidos se calculan, no se copian', () => {
    const barras = [{ precio: 130 }, { precio: 108 }, { precio: 122 }]
    expect(precioDesde(barras)).toBe(108)
    // Sube la más barata: el «desde» sube solo.
    expect(precioDesde([{ precio: 130 }, { precio: 122 }])).toBe(122)
  })

  it('precioDe encuentra el precio de un elemento por su clave', () => {
    const gotas = [
      { clave: 'jengibreYNaranja', precio: 340 },
      { clave: 'canela', precio: 258 },
    ]
    expect(precioDe(gotas, 'jengibreYNaranja')).toBe(340)
    expect(precioDe(gotas, 'canela')).toBe(258)
  })

  it('precioDe tira si la clave no existe, en vez de devolver undefined', () => {
    // Un undefined acá se renderiza como «$NaN» en la página. Mejor que
    // reviente el build.
    expect(() => precioDe([{ clave: 'canela', precio: 258 }], 'inventado')).toThrow(/inventado/)
  })
```

Sumá al import: `precioDesde, precioDe`.

- [ ] **Step 2: Correr para verificar que falla**

Run: `npx vitest run test/contenido.test.ts`
Expected: FAIL — no existen.

- [ ] **Step 3: Escribir `derivados.ts`**

```ts
/*
 * Los precios que NO se editan porque se calculan.
 *
 * El mismo número está escrito en hasta tres lugares del contenido de hoy:
 * el 108 en las barras y en la pestaña de negocios, el 258 en las gotas,
 * en `gotas.precioDesde` y en otra pestaña. Pedirle a la clienta que los
 * mantenga sincronizados a mano es pedirle que se equivoque — y el error
 * sale caro, porque el que queda viejo es el que ven las cafeterías.
 *
 * En el panel se dibujan en gris con la nota de dónde salen.
 */

/** El más barato de la lista. Alimenta todos los «desde $X». */
export function precioDesde(items: readonly { precio: number }[]): number {
  if (items.length === 0) throw new Error('No se puede calcular un «desde» de una lista vacía.')
  return Math.min(...items.map((i) => i.precio))
}

/** El precio de un elemento por su clave. Tira si no está. */
export function precioDe(
  items: readonly { clave: string; precio: number }[],
  clave: string,
): number {
  const encontrado = items.find((i) => i.clave === clave)
  if (!encontrado) {
    // Un undefined acá se renderiza «$NaN» en la página. Que reviente el
    // build es estrictamente mejor.
    throw new Error(`No hay ningún elemento con la clave «${clave}» para sacarle el precio.`)
  }
  return encontrado.precio
}
```

- [ ] **Step 4: Correr para verificar que pasa**

Run: `npx vitest run test/contenido.test.ts`
Expected: PASS, 22 tests.

- [ ] **Step 5: Probar que el derivado sigue al dato**

El test del Step 1 ya lo hace —sube la más barata y el «desde» cambia— pero comprobalo también al revés: cambiá temporalmente `precioDesde` para que devuelva una constante `108`.

Run: `npx vitest run test/contenido.test.ts -t 'se calculan'`
Expected: **FAIL** en el segundo caso (`expected 108 to be 122`). Confirma que el test detecta un derivado que dejó de derivar.

Restaurá. Pegá las dos salidas.

- [ ] **Step 6: Correr la suite y commitear**

Run: `pnpm test && pnpm typecheck`
Expected: PASS — 705 tests, 0 errores.

```bash
git add src/contenido/derivados.ts test/contenido.test.ts
git commit -m "feat: los precios repetidos se calculan, no se copian

El 108 está escrito en las barras Y en la pestaña de negocios; el 258 en
las gotas, en gotas.precioDesde Y en otra pestaña. Si la clienta sube las
barras desde el anaquel, hoy la pestaña «Para negocios» le sigue diciendo
«desde \$108» a las cafeterías.

precioDe tira si la clave no existe: un undefined ahí se renderiza «\$NaN»
en la página, y que reviente el build es estrictamente mejor.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Task 8: `validacion.ts` — una verdad, cuatro consumidores

La pieza que hace que el panel, la función serverless, vitest y `astro build` no puedan tener criterios distintos: los cuatro importan **el mismo archivo**.

En la Parte A se escribe el traductor —de un error de Zod a un problema que la clienta entiende— y se prueba con esquemas de juguete. La Parte B le suma el registro de documentos y las ~25 mutaciones reales.

**Files:**
- Create: `src/contenido/validacion.ts`
- Modify: `test/contenido.test.ts`

**Interfaces:**
- Consumes: nada de tareas previas — `validarContra` trabaja sobre el esquema que le pasan.
- Produces: `Problema`, `validarContra(esquema, crudo): Problema[]`. En la Parte B, `validar(doc, crudo)` lo envuelve resolviendo el esquema por documento.

- [ ] **Step 1: Escribir los tests que fallan**

```ts
  it('traduce el error de Zod a algo que la clienta entiende, con la ruta para ir', () => {
    const esquema = grupo({
      etiqueta: 'x', seccion: 'portada', ayuda: 'x',
      campos: {
        hero: grupo({
          etiqueta: 'Portada', seccion: 'portada', ayuda: 'y',
          campos: { sub: texto({ etiqueta: 'Bajada', seccion: 'portada', ayuda: 'z', max: 10 }) },
        }),
      },
    })
    const problemas = validarContra(esquema, { hero: { sub: 'x'.repeat(30) } })
    expect(problemas).toHaveLength(1)
    // La ruta punteada es lo que le permite al panel llevarla al campo.
    expect(problemas[0].campo).toBe('hero.sub')
    expect(problemas[0].gravedad).toBe('impide')
    // Y el título no tiene jerga: nada de «String must contain at most».
    expect(problemas[0].titulo).not.toMatch(/string|expected|invalid/i)
  })

  it('un contenido válido no genera ni un problema', () => {
    const esquema = grupo({
      etiqueta: 'x', seccion: 'portada', ayuda: 'x',
      campos: { t: texto({ etiqueta: 'T', seccion: 'portada', ayuda: 'y', max: 30 }) },
    })
    expect(validarContra(esquema, { t: 'Chocolate mexicano' })).toEqual([])
  })

  it('ofrece el arreglo de un toque cuando lo hay: el espacio que no parte', () => {
    const esquema = grupo({
      etiqueta: 'x', seccion: 'productos', ayuda: 'x',
      campos: { p: medida({ etiqueta: 'Peso', seccion: 'productos', ayuda: 'y', max: 30 }) },
    })
    const problemas = validarContra(esquema, { p: 'Gotas de 250 g' })
    expect(problemas).toHaveLength(1)
    expect(problemas[0].arreglo?.valor).toBe('Gotas de 250 g')
    expect(problemas[0].arreglo?.etiqueta).toMatch(/espacio/i)
  })
```

Sumá al import: `validarContra`.

- [ ] **Step 2: Correr para verificar que falla**

Run: `npx vitest run test/contenido.test.ts`
Expected: FAIL — `validarContra` no existe.

- [ ] **Step 3: Escribir `validacion.ts`**

```ts
/*
 * Una verdad, cuatro consumidores.
 *
 * Los cuatro importan ESTE archivo: el navegador mientras la clienta
 * escribe, `api/panel.ts` antes de tocar GitHub, vitest, y `astro build`
 * (porque `cargar()` está en el camino del import). Si cada uno tuviera su
 * criterio, el panel diría que sí y el build diría que no — que es la peor
 * experiencia posible: ella publica y le rebota sin saber por qué.
 */
import type { z } from 'zod'

export interface Problema {
  /** Ruta punteada: 'sabores.3.nombre'. El panel la usa para llevarla al campo. */
  campo: string
  gravedad: 'impide' | 'avisa'
  /** Lo que lee la clienta. Sin jerga, sin nombres de tipos. */
  titulo: string
  detalle?: string
  /** Un botón de un toque, cuando el arreglo es obvio. */
  arreglo?: { etiqueta: string; valor: unknown }
}

/** El espacio duro entre cifra y unidad, que un input común se come. */
// Mismo cuidado que en el constructor `medida`: `\s` incluiría el espacio
// duro y propondría «arreglar» un valor que ya está correcto.
const CIFRA_UNIDAD = /(\d)[^\S\u00a0](g|kg|ml|l|°C)\b/g

function proponeArreglo(mensaje: string, valor: unknown): Problema['arreglo'] {
  if (typeof valor === 'string' && CIFRA_UNIDAD.test(valor)) {
    CIFRA_UNIDAD.lastIndex = 0
    return {
      etiqueta: 'Poner el espacio que no parte el renglón',
      valor: valor.replace(CIFRA_UNIDAD, '$1 $2'),
    }
  }
  return undefined
}

function enRuta(crudo: unknown, ruta: readonly PropertyKey[]): unknown {
  let actual: unknown = crudo
  for (const paso of ruta) {
    if (actual === null || typeof actual !== 'object') return undefined
    actual = (actual as Record<PropertyKey, unknown>)[paso]
  }
  return actual
}

/** Valida un contenido contra su esquema y devuelve problemas legibles. */
export function validarContra(esquema: z.ZodType, crudo: unknown): Problema[] {
  const r = esquema.safeParse(crudo)
  if (r.success) return []

  return r.error.issues.map((issue) => {
    const campo = issue.path.join('.')
    const valor = enRuta(crudo, issue.path)
    return {
      campo,
      gravedad: 'impide' as const,
      titulo: issue.message,
      arreglo: proponeArreglo(issue.message, valor),
    }
  })
}
```

- [ ] **Step 4: Correr para verificar que pasa**

Run: `npx vitest run test/contenido.test.ts`
Expected: PASS, 25 tests.

- [ ] **Step 5: Probar que la ausencia de jerga es real y no suerte**

El test afirma que el título no dice «string», «expected» ni «invalid». Comprobá que eso lo garantizan los mensajes que escribimos en los constructores y no el azar: cambiá temporalmente el `.max(meta.max)` de `texto` para que no lleve mensaje propio (o sea, dejá que Zod ponga el suyo en inglés).

Run: `npx vitest run test/contenido.test.ts -t 'entiende'`
Expected: **FAIL** — el título trae el mensaje en inglés de Zod.

Restaurá — y si al restaurar descubrís que `texto` **no** tiene un mensaje propio para el `max`, eso es un hueco real del plan: **agregalo** (algo como `'Te pasaste del tope de seguridad de N caracteres.'`) y decilo en el reporte.

- [ ] **Step 6: Correr la suite y commitear**

Run: `pnpm test && pnpm typecheck`
Expected: PASS — 708 tests, 0 errores.

```bash
git add src/contenido/validacion.ts test/contenido.test.ts
git commit -m "feat: la validación, una verdad para cuatro consumidores

El navegador mientras la clienta escribe, api/panel.ts antes de tocar
GitHub, vitest y astro build importan el MISMO archivo. Con criterios
distintos, el panel diría que sí y el build diría que no: ella publica y
le rebota sin saber por qué.

Los problemas salen con la ruta punteada (para que el panel la lleve al
campo), sin jerga, y con el arreglo de un toque cuando es obvio — el
espacio duro entre cifra y unidad, que un input común se come y deja la
«g» sola en el renglón siguiente del celular.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

## Cierre de la Parte A

Al terminar las ocho tareas existe `src/contenido/` completo y probado:

- La regla de la carpeta, vigilada: nada de `node:*`, Astro ni `@/`.
- Los constructores, que **exigen** etiqueta y ayuda en español.
- `recorre()`, con su canario sobre los internals de Zod.
- `cargar()` (valida, congela, tira) y `serializa()` (bytes canónicos, y prueba de completitud).
- Las reglas de conteo y de contraste, cada una en un solo lugar.
- Los derivados y el traductor de problemas.

**Lo que NO hace la Parte A:** ningún esquema real, ningún JSON, ninguna fachada. El sitio no cambia: `index.astro` sigue leyendo los módulos `as const` de hoy y `pnpm build` sigue verde. Es una biblioteca sin consumidor, a propósito.

### Dos cosas que la Parte A deja declaradas y la Parte B tiene que cubrir

No son huecos del plan: son reglas que dependen de campos concretos y que
por eso se declaran en el esquema, no en los constructores. Quedan acá
escritas para que la Parte B no las olvide.

1. **El espacio duro en los siete campos de texto libre.** El constructor
   `medida` cubre el único campo que es «cifra + unidad» puro
   (`anaquel.pesoInsignia`). Los otros siete son texto con la cifra adentro
   —«Chocolate en polvo · 250 g y 1 kg»— y necesitan la misma regla
   aplicada en su declaración.
2. **Las reglas de un solo campo:** el renglón 2 del titular
   (`/^[^,]+,$/` — exactamente una coma, al final), el `<` prohibido en
   todo campo que viaje a `set:html` o al JSON-LD, `& < > "` en
   `marca.titulo` y `marca.descripcion`, el tope de 155 de `descripcion`
   (Google corta), y el prefijo exacto de los ingredientes contra el arte
   de imprenta.

**Siguiente:** `docs/superpowers/plans/2026-09-XX-panel-fase-1-parte-b.md` — los esquemas (`sitio`, `sabores`, `fichas`), el script de migración, las tres fachadas y el certificado permanente de que la migración no movió una coma. Se escribe cuando la Parte A esté revisada, contra la API que quedó de verdad.
